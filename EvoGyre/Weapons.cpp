#include "Weapons.h"


//Texture WeaponSystem::blueLaser = Texture(FILENAME::BLUELASER);
/*
vector<Texture> WeaponSystem::weaponTextures({
	Texture(FILENAME::BLUELASER)
	//Texture(FILENAME::REDLASER),
	//Texture(FILENAME::ROUND_LG),
	//Texture(FILENAME::ROUND_SM),
	//Texture(FILENAME::SHARPLASER),
});

Texture WeaponSystem::GetWeaponTexture(int e)
{
	return weaponTextures[e];
}
*/
